name: Second Workflow

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps: 
      - name: say hello
        run: echo "Hello, World!"

      - name: use env variables
        run: echo "Application Environment is $APP_ENV"
        env:
          APP_ENV: "UAT"